spring:
  application:
    name: users
  cloud:
    config:
      uri: http://localhost:9090
      label: master
      profile: developer
      enabled: true
  rabbitmq:
    host: localhost
    username: user
    password: "bitnami"
    port: 5672
server:
  port: 8081
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    fetch-registry: true
    register-with-eureka: true
